<Window x:Class="JarvisTeto.FloatingWidget"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ctrl="clr-namespace:JarvisTeto.Controls"
        Title="Jarvis"
        Width="128" Height="128"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="Manual"
        WindowStartupLocation="Manual"
        MouseLeftButtonDown="Widget_MouseLeftButtonDown">

    <!-- Click derecho: acceso rápido sin tener que abrir la ventana completa -->
    <Window.ContextMenu>
        <ContextMenu Background="{StaticResource BgPanel}" BorderBrush="{StaticResource BorderBlue}" BorderThickness="1">
            <MenuItem Header="Chatear" Click="OpenBubble_Click" Foreground="{StaticResource TextPrimary}"/>
            <MenuItem Header="Ventana completa" Click="OpenFullWindow_Click" Foreground="{StaticResource TextPrimary}"/>
            <Separator/>
            <MenuItem Header="Ocultar widget" Click="HideWidget_Click" Foreground="{StaticResource TextPrimary}"/>
            <MenuItem Header="Salir" Click="Exit_Click" Foreground="{StaticResource TextPrimary}"/>
        </ContextMenu>
    </Window.ContextMenu>

    <Grid>
        <!-- "pequeño pero no tan pequeño": deja algo de aire dentro de la ventana de 128x128
             para que el glow de la esfera no se corte contra el borde. -->
        <ctrl:OrbVisualizer x:Name="Orb" Width="112" Height="112"
                             HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</Window>
